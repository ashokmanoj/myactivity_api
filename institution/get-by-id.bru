meta {
  name: Get institution By ID
  type: http
  seq: 2
}
get {
  url: {{base_url}}/api/v1/institution/{{institute_id}}
  body: none
  auth: none
}
headers {
  Content-Type: application/json
}
tests {
  test("Status 200", function() { expect(res.getStatus()).to.equal(200); });
  test("has instituteId", function() { expect(res.getBody().data).to.have.property("instituteId"); });
  test("has project", function() { expect(res.getBody().data).to.have.property("project"); });
}
