meta {
  name: SETTINGS-02: Get Projects
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/settings/projects
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Returns all active projects for the module settings dropdown.

  Response:
  - data[]: id, name, code
}

tests {
  test("Status 200", function() { expect(res.getStatus()).to.equal(200); });
  test("success true", function() { expect(res.getBody().success).to.equal(true); });
  test("has array", function() { expect(res.getBody().data).to.be.an("array"); });
}
